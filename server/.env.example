PORT=3001
NODE_ENV=development
DB_PATH=../sqlite/nutrition.db
ADMIN_SECRET=secret_admin_token_123456
CORS_ORIGIN=http://localhost:5173
# NOTE: Le serveur Express tourne sur le port 3001 (API only).
# L'interface frontend (Vite dev server) est sur http://localhost:5173.
# C'est donc http://localhost:5173 qu'il faut ouvrir dans le navigateur.
